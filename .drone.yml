pipeline:
  test:
    image: majexa/ngn
    commands:
      - /etc/init.d/mysql start
      - sudo -u user bash
      - cp -arf /drone/src/github.com/majexa/pm /home/user/ngn-env
      - cp /home/user/ngn-env /root
#      - chown -R user:user /home/user/ngn-env/pm
      - pm localServer createDatabaseConfig
      - pm localServer createProject test test.local common
#      - su user pm localProjects updateIndex
